VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id

# Set to true to show a "Continue as Dev User" button on the login page.
# Requires ENVIRONMENT=development in backend/.env. Never set in production.
VITE_DEV_BYPASS_AUTH=false
