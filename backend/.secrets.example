# Supabase database passwords — copy to .secrets and fill in
# Find these in: Supabase Dashboard → Settings → Database → Connection string
SUPABASE_DB_PASSWORD_APAC=your-apac-db-password
SUPABASE_DB_PASSWORD_NOAM=your-noam-db-password
